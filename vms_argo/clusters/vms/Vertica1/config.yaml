application:
  ignoreDifferences:
    - group: 'kubevirt.io'
      kind: VirtualMachine
      jsonPointers:
      - /spec
    - group: ''
      kind: Secret
      jsonPointers:
      - /spec
  name: "vertica1"
  namespace: "default"
  componentName: Vertica
target:
  targetRevision: "datavolume-cluster"
  path: "vms_argo/helmcharts/Vertica1"
deploy: false
cluster:
  name: in-cluster
  address: "https://kubernetes.default.svc"
