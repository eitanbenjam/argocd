apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: helm-server
    app.kubernetes.io/name: helm-server
  name: helm-server-node-port
spec:
  type: NodePort
  ports:
  - name: server
    port: 8080
    protocol: TCP
    nodePort: 30006
  selector:
    app.kubernetes.io/name: ace-cd-helm-repository-deployment

