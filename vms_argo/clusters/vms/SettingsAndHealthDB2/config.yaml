application:
  ignoreDifferences:
    - group: 'kubevirt.io'
      kind: VirtualMachine
      jsonPointers:
      - /spec
    - group: ''
      kind: Secret
      jsonPointers:
      - /spec
  name: "mongo2"
  namespace: "default"
  componentName: SettingsAndHealthDB
target:
  targetRevision: "datavolume-cluster"
  path: "vms_argo/helmcharts/SettingsAndHealthDB2"
deploy: false
cluster:
  name: in-cluster
  address: "https://kubernetes.default.svc"
