application:
  ignoreDifferences:
    - group: 'kubevirt.io'
      kind: VirtualMachine
      jsonPointers:
      - /spec
    - group: ''
      kind: Secret
      jsonPointers:
      - /spec
  name: "settingsandhealthdb"
  namespace: "default"
target:
  targetRevision: "eitan"
  path: "vms_argo/helmcharts/SettingsAndHealthDB"
deploy: true
cluster:
  name: in-cluster
  address: "https://kubernetes.default.svc"
