application:
  ignoreDifferences:
    - group: 'kubevirt.io'
      kind: VirtualMachine
      jsonPointers:
      - /spec
    - group: ''
      kind: Secret
      jsonPointers:
      - /spec
  name: "kafka0"
  namespace: "default"
  componentName: Kafka
target:
  targetRevision: "datavolume-cluster"
  path: "vms_argo/helmcharts/Kafka0"
deploy: false
cluster:
  name: in-cluster
  address: "https://kubernetes.default.svc"
