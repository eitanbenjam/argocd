application:
  name: "k8s-infra"
  namespace: "default"
target:
  targetRevision: "datavolume-cluster"
  path: "containters/helmcharts/bp-k8s-infra"
deploy: true
cluster:
  name: in-cluster
  adress: "https://kubernetes.default.svc"
