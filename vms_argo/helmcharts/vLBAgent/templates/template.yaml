apiVersion: v1
kind: Secret
metadata:
  name: vlbagent-{{ .Values.machine_index }}-cloudinit-secret
  namespace: {{ .Values.general.namespace }}
type: Opaque
data:
  userdata: {{ (tpl (.Files.Get "userdata/startup-script") . | b64enc ) }}
---
apiVersion: v1
kind: Service
metadata:
  name: vlbagent-{{ .Values.machine_index }}-service
spec:
  selector:
    radcom-machine: vlbagent-{{ .Values.machine_index }}
  ports:
  - name: ssh
    port: 22

---           
apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: vlbagent-{{ .Values.machine_index }}
  annotations:
    description: vLBAgent
    k8s.v1.cni.cncf.io/networks: pktmirror-net,pktinternal-net,cdr-direct-net,
  labels:
    app: vlbagent
    os.template.kubevirt.io/fedora: 'true'
  namespace: {{ .Values.general.namespace }}
spec:
  running: true
  dataVolumeTemplates:
  - metadata:
      name: vlbagent-{{ .Values.machine_index }}-rootdisk
    spec:
      storage:
        storageClassName: {{ .Values.general.storageClassName }}
        accessModes:
        - ReadWriteOnce
        resources:
          requests:
            storage: 20Gi
      source:
        pvc:
          namespace: {{ .Values.general.namespace }}
          name: {{ .Values.general.rocky_pvc_name }}

  template:
    metadata:
      annotations:        
        vm.kubevirt.io/os: Rocky9
        vm.kubevirt.io/workload: server
      labels:
        app: vlbagent
        radcom-machine: vlbagent-{{ .Values.machine_index }}
        kubevirt.io/domain: radcom
        kubevirt.io/size: small
    spec:
      {{- if .Values.node_selector_hostname }}
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution: 
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: In
                values:
                - {{ .Values.node_selector_hostname }}
      {{- end }}
      domain:
        cpu:
          cores: 4
          sockets: 1
          threads: 1
        devices:
          disks:
            - disk:
                bus: sata
              bootOrder: 1
              name: rootdisk
            - disk:
                bus: virtio
              name: cloudinitdisk
            
            - disk:
                bus: sata
              name: vlbagent-{{ .Values.machine_index }}-data
              serial: data
           
            - disk:
                bus: sata
              name: vlbagent-{{ .Values.machine_index }}-log
              serial: log
           
            - disk:
                bus: sata
              name: vlbagent-{{ .Values.machine_index }}-config
              serial: config
             
          interfaces:
         
           
            - name: oam-direct
              masquerade: {}
              model: virtio              
              
         
           
            - bridge: {}
              model: virtio
              name: pktmirror           
        
         
           
            - bridge: {}
              model: virtio
              name: pktinternal           
        
         
           
            - bridge: {}
              model: virtio
              name: cdr-direct           
        
         
          networkInterfaceMultiqueue: true
          rng: {}
        features:
          acpi: {}
          smm:
            enabled: true
        machine:
          type: q35
        memory:
          guest: 8192M
      hostname: vlbagent-{{ .Values.machine_index }}
      networks:
       
       
       - name: oam-direct
         pod: {}
        
        
       
       - name: pktmirror
         multus:
           networkName: default/pktmirror-net           
        
        
       
       - name: pktinternal
         multus:
           networkName: default/pktinternal-net           
        
        
       
       - name: cdr-direct
         multus:
           networkName: default/cdr-direct-net           
        
        
      terminationGracePeriodSeconds: 10
      volumes:
        - dataVolume:
            name: vlbagent-{{ .Values.machine_index }}-rootdisk
          name: rootdisk
  
        
        - name: vlbagent-{{ .Values.machine_index }}-data
          persistentVolumeClaim:
            claimName: vlbagent-{{ .Values.machine_index }}-data-pvc
        
        - name: vlbagent-{{ .Values.machine_index }}-log
          persistentVolumeClaim:
            claimName: vlbagent-{{ .Values.machine_index }}-log-pvc
        
        - name: vlbagent-{{ .Values.machine_index }}-config
          persistentVolumeClaim:
            claimName: vlbagent-{{ .Values.machine_index }}-config-pvc
         
        
        - name: cloudinitdisk
          cloudInitNoCloud:
            secretRef:
              name: vlbagent-{{ .Values.machine_index }}-cloudinit-secret




---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: vlbagent-{{ .Values.machine_index }}-data-pvc
spec:
  accessModes:
  - ReadWriteOnce
  volumeMode: Filesystem 
  resources:
    requests:
      storage: 5Gi
  storageClassName: {{ .Values.general.storageClassName }}
  

---
{{- if .Values.node_selector_hostname }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: vlbagent-{{ .Values.machine_index }}-data-pv
spec:
  capacity:
    storage: 50Gi
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Delete
  claimRef:
     namespace: {{ .Values.general.namespace }}
     name: vlbagent-{{ .Values.machine_index }}-data-pvc

  storageClassName: {{ .Values.general.storageClassName }}
  local:
    path: /mnt/data/vlbagent-{{ .Values.machine_index }}-data-pvc/
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - {{ .Values.node_selector_hostname }}
{{- end }}
---


---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: vlbagent-{{ .Values.machine_index }}-log-pvc
spec:
  accessModes:
  - ReadWriteOnce
  volumeMode: Filesystem 
  resources:
    requests:
      storage: 1Gi
  storageClassName: {{ .Values.general.storageClassName }}
  

---
{{- if .Values.node_selector_hostname }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: vlbagent-{{ .Values.machine_index }}-log-pv
spec:
  capacity:
    storage: 50Gi
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Delete
  claimRef:
     namespace: {{ .Values.general.namespace }}
     name: vlbagent-{{ .Values.machine_index }}-log-pvc

  storageClassName: {{ .Values.general.storageClassName }}
  local:
    path: /mnt/data/vlbagent-{{ .Values.machine_index }}-log-pvc/
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - {{ .Values.node_selector_hostname }}
{{- end }}
---


---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: vlbagent-{{ .Values.machine_index }}-config-pvc
spec:
  accessModes:
  - ReadWriteOnce
  volumeMode: Filesystem 
  resources:
    requests:
      storage: 1Gi
  storageClassName: {{ .Values.general.storageClassName }}
  

---
{{- if .Values.node_selector_hostname }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: vlbagent-{{ .Values.machine_index }}-config-pv
spec:
  capacity:
    storage: 50Gi
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Delete
  claimRef:
     namespace: {{ .Values.general.namespace }}
     name: vlbagent-{{ .Values.machine_index }}-config-pvc

  storageClassName: {{ .Values.general.storageClassName }}
  local:
    path: /mnt/data/vlbagent-{{ .Values.machine_index }}-config-pvc/
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - {{ .Values.node_selector_hostname }}
{{- end }}
---

