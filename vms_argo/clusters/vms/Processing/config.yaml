application:
  ignoreDifferences:
    - group: 'kubevirt.io'
      kind: VirtualMachine
      jsonPointers:
      - /spec
    - group: ''
      kind: Secret
      jsonPointers:
      - /spec
  name: "processing"
  namespace: "default"
target:
  targetRevision: "eitan"
  path: "vms_argo/helmcharts/Processing"
deploy: true
cluster:
  name: in-cluster
  address: "https://kubernetes.default.svc"
