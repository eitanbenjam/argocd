application:
  ignoreDifferences:
    - group: 'kubevirt.io'
      kind: VirtualMachine
      jsonPointers:
      - /spec
    - group: ''
      kind: Secret
      jsonPointers:
      - /spec
  name: "mongo1"
  namespace: "default"
  componentName: SettingsAndHealthDB
target:
  targetRevision: "datavolume-cluster"
  path: "vms_argo/helmcharts/SettingsAndHealthDB1"
deploy: true
cluster:
  name: in-cluster
  address: "https://kubernetes.default.svc"
