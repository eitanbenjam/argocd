apiVersion: v1
kind: Secret
metadata:
  name: kuberiq-{{ .Values.machine_index }}-cloudinit-secret
  namespace: {{ .Values.general.namespace }}
type: Opaque
data:
  userdata: {{ (tpl (.Files.Get "userdata/startup-script") . | b64enc ) }}
---
apiVersion: v1
kind: Service
metadata:
  name: kuberiq-{{ .Values.machine_index }}-service
spec:
  selector:
    radcom-machine: kuberiq-{{ .Values.machine_index }}
  ports:
  - name: ssh
    port: 22

---           
apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: kuberiq-{{ .Values.machine_index }}
  annotations:
    description: Kuberiq
    k8s.v1.cni.cncf.io/networks: backend-net,cdr-direct-net,oam-protected-net,
  labels:
    app: kuberiq
    os.template.kubevirt.io/fedora: 'true'
  namespace: {{ .Values.general.namespace }}
spec:
  running: true
  dataVolumeTemplates:
  - metadata:
      name: kuberiq-{{ .Values.machine_index }}-rootdisk
    spec:
      storage:
        storageClassName: {{ .Values.general.storageClassName }}
        accessModes:
        - ReadWriteOnce
        resources:
          requests:
            storage: 20Gi
      source:
        pvc:
          namespace: {{ .Values.general.namespace }}
          name: {{ .Values.general.rocky_pvc_name }}

  template:
    metadata:
      annotations:        
        vm.kubevirt.io/os: Rocky9
        vm.kubevirt.io/workload: server
      labels:
        app: kuberiq
        radcom-machine: kuberiq-{{ .Values.machine_index }}
        kubevirt.io/domain: radcom
        kubevirt.io/size: small
    spec:
      {{- if .Values.node_selector_hostname }}
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution: 
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: In
                values:
                - {{ .Values.node_selector_hostname }}
      {{- end }}
      domain:
        cpu:
          cores: 16
          sockets: 1
          threads: 1
        devices:
          disks:
            - disk:
                bus: sata
              bootOrder: 1
              name: rootdisk
            - disk:
                bus: virtio
              name: cloudinitdisk
            
            - disk:
                bus: sata
              name: kuberiq-{{ .Values.machine_index }}-base
              serial: base
             
          interfaces:
         
           
            - name: oam-direct
              masquerade: {}
              model: virtio              
              
         
           
            - bridge: {}
              model: virtio
              name: backend           
        
         
           
            - bridge: {}
              model: virtio
              name: cdr-direct           
        
         
           
            - bridge: {}
              model: virtio
              name: oam-protected           
        
         
          networkInterfaceMultiqueue: true
          rng: {}
        features:
          acpi: {}
          smm:
            enabled: true
        machine:
          type: q35
        memory:
          guest: 32968M
      hostname: kuberiq-{{ .Values.machine_index }}
      networks:
       
       
       - name: oam-direct
         pod: {}
        
        
       
       - name: backend
         multus:
           networkName: default/backend-net           
        
        
       
       - name: cdr-direct
         multus:
           networkName: default/cdr-direct-net           
        
        
       
       - name: oam-protected
         multus:
           networkName: default/oam-protected-net           
        
        
      terminationGracePeriodSeconds: 10
      volumes:
        - dataVolume:
            name: kuberiq-{{ .Values.machine_index }}-rootdisk
          name: rootdisk
  
        
        - name: kuberiq-{{ .Values.machine_index }}-base
          persistentVolumeClaim:
            claimName: kuberiq-{{ .Values.machine_index }}-base-pvc
         
        
        - name: cloudinitdisk
          cloudInitNoCloud:
            secretRef:
              name: kuberiq-{{ .Values.machine_index }}-cloudinit-secret




---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: kuberiq-{{ .Values.machine_index }}-base-pvc
spec:
  accessModes:
  - ReadWriteOnce
  volumeMode: Filesystem 
  resources:
    requests:
      storage: 300Gi
  storageClassName: {{ .Values.general.storageClassName }}
  

---
{{- if .Values.node_selector_hostname }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: kuberiq-{{ .Values.machine_index }}-base-pv
spec:
  capacity:
    storage: 50Gi
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Delete
  claimRef:
     namespace: {{ .Values.general.namespace }}
     name: kuberiq-{{ .Values.machine_index }}-base-pvc

  storageClassName: {{ .Values.general.storageClassName }}
  local:
    path: /mnt/data/kuberiq-{{ .Values.machine_index }}-base-pvc/
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - {{ .Values.node_selector_hostname }}
{{- end }}
---

