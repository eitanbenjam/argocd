application:
  name: "probe-lb-agent"
  namespace: "default"
target:
  targetRevision: "datavolume-cluster"
  path: "containters/helmcharts/probe-lb-agent"
deploy: false
cluster:
  name: in-cluster
  adress: "https://kubernetes.default.svc"