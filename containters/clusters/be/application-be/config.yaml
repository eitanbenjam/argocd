application:
  name: "bp-application-be"
  namespace: "default"
  ignoreDifferences:
  - group: "apps"
    kind: "StatefulSet"
    jqPathExpressions:
      - ".spec.volumeClaimTemplates[].apiVersion"
      - ".spec.volumeClaimTemplates[].kind"
target:
  targetRevision: "eitan"
  path: "containters/helmcharts/bp-application-be"
deploy: true
cluster:
  name: in-cluster
  adress: "https://kubernetes.default.svc"
