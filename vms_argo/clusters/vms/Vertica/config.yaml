application:
  ignoreDifferences:
    - group: 'kubevirt.io'
      kind: VirtualMachine
      jsonPointers:
      - /spec
    - group: ''
      kind: Secret
      jsonPointers:
      - /spec
  name: "vertica"
  namespace: "default"
target:
  targetRevision: "eitan"
  path: "vms_argo/helmcharts/Vertica"
deploy: false
cluster:
  name: in-cluster
  address: "https://kubernetes.default.svc"
