application:
  ignoreDifferences:
    - group: 'kubevirt.io'
      kind: VirtualMachine
      jsonPointers:
      - /spec
    - group: ''
      kind: Secret
      jsonPointers:
      - /spec
  name: "conductor0"
  namespace: "default"
target:
  targetRevision: datavolume-cluster
  path: "vms_argo/helmcharts/Conductor0"
deploy: true
cluster:
  name: in-cluster
  address: "https://kubernetes.default.svc"
