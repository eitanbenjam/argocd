application:
  name: "bp-admin-applications"
  namespace: "default"
target:
  targetRevision: "datavolume-cluster"
  path: "containters/helmcharts/bp-admin-applications"
deploy: true
cluster:
  name: in-cluster
  adress: "https://kubernetes.default.svc"
